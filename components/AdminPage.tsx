import React from 'react'
import styled from 'styled-components'

const AdminPageContainer = styled.div`
  margin-top: 30px;
  margin-left: 50px;
  clear:both;
  float: left;
`

const AdminPage = (): JSX.Element => {
  return <AdminPageContainer>Admin</AdminPageContainer>
}

export default AdminPage
