import React from 'react'
import styled from 'styled-components'

const ParamPageContainer = styled.div`
  margin-top: 30px;
  margin-left: 50px;
  clear:both;
  float: left;
`

const ParamPage = (): JSX.Element => {
  return <ParamPageContainer>Parametros</ParamPageContainer>
}

export default ParamPage
